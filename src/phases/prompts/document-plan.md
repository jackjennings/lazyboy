This ticket produces a document, not a code change.

Your plan must contain:

1. The full document text in Markdown, ready to upload as-is.
2. The parent Notion page URL where the document will be created (from the
   spec).

Do not include implementation steps, code tasks, or git commit instructions.
There are no worktrees or pull requests. The plan IS the document content.
