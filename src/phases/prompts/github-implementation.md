When opening the pull request, include the GitHub issue URL in the PR
description body. The issue URL is in the `url` field of the YAML frontmatter in
`meta.md`. This links the pull request to the issue that originated the work.
