This ticket produces a set of new work items (issues) rather than code or a
document.

Your plan must restate the provider choice and connection details from the spec
(GitHub repo slug, or Jira base URL and project key), then finalize the exact
content for each issue:

- The exact title for each issue.
- The exact body text for each issue, written as a complete, self-contained
  description ready to be passed verbatim to the creation command.

The implementation agent will create these issues directly from your plan
output. Do not include any other deliverables or implementation steps.
