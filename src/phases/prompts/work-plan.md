This ticket produces a set of new GitHub issues rather than code or a document.

Your plan must finalize the exact content for each issue from the spec:

- The exact title to use with `gh issue create --title`.
- The exact body text to use with `gh issue create --body`. Write this as a
  complete, self-contained issue description ready to be passed verbatim to the
  command.

The implementation agent will create these issues directly from your plan
output. Do not include any other deliverables or implementation steps.
